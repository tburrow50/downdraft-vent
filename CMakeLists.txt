add_executable(vent
        vent.c
        )

# pull in common dependencies
target_link_libraries(vent pico_stdlib)

# create map/bin/hex file etc.
pico_add_extra_outputs(vent)

# add url via pico_set_program_url
example_auto_set_url(vent)
